<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.sportslightadmin.video.dao.VideoDAO">
	<select id="count" resultType="int">
		SELECT count(*) FROM video
	</select>


	<select id="videoList" parameterType="VideoPageDTO" resultType="VideoDTO">
	<![CDATA[ 
		SELECT * FROM video
		where videoNum >=#{startRow} and videoNum <=#{endRow}
		]]>
	</select>

	<!--상세 페이지-->
	<select id="videoContent" parameterType="int" resultType="VideoDTO">
		SELECT * FROM video
		where videoNum = #{videoNum}
	</select>

	<!--수정-->
	<update id="update" parameterType="VideoDTO"> 
		update video
		set videoName= #{videoName, jdbcType=VARCHAR}, thumbnailUrl= #{thumbnailUrl, jdbcType=VARCHAR}, 
		videoUrl=#{videoUrl, jdbcType=VARCHAR}, videoType=#{videoType}, teamNum1=#{teamNum1}, teamNum2=#{teamNum2}, seasonNum=#{seasonNum}
		where videoNum =#{videoNum}
	</update>
	
	<!--삭제-->
	<delete id="delete" parameterType="int">
		DELETE FROM video
		WHERE videoNum=#{videoNum}
	</delete>
	

</mapper>



